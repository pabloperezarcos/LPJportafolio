//Esta interface la utilizaremos para manejar un arreglo
//llamado componente para manejar el menú slide de las vistas.

export interface Componente {
    icon: string;
    name: string;
    redirectTo: string;
}